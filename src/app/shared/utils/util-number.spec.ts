import {UtilNumber} from './util-number';

describe('UtilNumber Tests', () => {
	
	beforeEach(() => {
		//
	});
	
	it('getRandomBetween should be defined', () => {
		expect(UtilNumber.getRandomBetween).toBeDefined();
	});
});