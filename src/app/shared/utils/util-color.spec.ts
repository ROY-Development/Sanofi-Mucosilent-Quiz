import {UtilColor} from './util-color';

describe('UtilColor Tests', () => {
	
	beforeEach(() => {
		//
	});
	
	it('getInTransparentColorFromColor should be defined', () => {
		expect(UtilColor.getInTransparentColorFromColor).toBeDefined();
	});
});