import {UtilArray} from './util-array';

describe('UtilArray Tests', () => {
	
	beforeEach(() => {
		//
	});
	
	it('moveElement should be defined', () => {
		expect(UtilArray.moveElement).toBeDefined();
	});
	
	it('groupBy should be defined', () => {
		expect(UtilArray.groupBy).toBeDefined();
	});
});